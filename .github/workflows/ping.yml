name: Ping API

on:
  schedule:
    - cron: "*/5 * * * *" # var 5:e minut

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl -I https://iotdataportal-api-g6asahc9emdkgdd0.swedencentral-01.azurewebsites.net/health
